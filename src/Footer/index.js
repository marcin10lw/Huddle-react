import { StyledFooter, StylingImage } from "./styled";

const Footer = () => {
  return (
    <>
      <StylingImage
        src={`${process.env.PUBLIC_URL}/assets/images/bg-footer-top-desktop.svg`}
      />
      <StyledFooter>
        
      </StyledFooter>
    </>
  );
};

export default Footer;
